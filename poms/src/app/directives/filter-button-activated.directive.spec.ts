import { FilterButtonActivatedDirective } from './filter-button-activated.directive';

describe('FilterButtonActivatedDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterButtonActivatedDirective();
    expect(directive).toBeTruthy();
  });
});
